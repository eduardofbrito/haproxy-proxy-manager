{% if caching_enabled == 1 or caching_enabled == true -%}
# Asset Caching
acl is_static_ext path_end .css .js .gif .png .jpg .jpeg .ico .svg .woff .woff2 .ttf .eot
cache use static_cache if is_static_ext
{% endif %}
